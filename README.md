This program demonstrates a custom 'ping' using the ICMP protocol. Note that this program requires root privileges to run due to the use of raw sockets.

Usage: ```sudo python3 ./pgm3.py <ip address>```
